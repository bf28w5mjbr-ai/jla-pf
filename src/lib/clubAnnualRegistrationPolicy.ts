/**
 * クラブの年度登録（`ClubAnnualRegistration` の PAID 要件）を有効にするか。
 * `false` のあいだは、参加・承認フローや種別更新スケジュールから年度登録を参照しない（一時停止）。
 * 再有効化するときは `true` にし、必要ならデータを整える。
 */
export const CLUB_ANNUAL_REGISTRATION_ENABLED = false;
