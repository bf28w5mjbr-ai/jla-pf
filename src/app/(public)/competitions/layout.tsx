import type { ReactNode } from "react";

/** ヘッダー・サイドバーは (public)/layout の PublicSiteShell に統合 */
export default function PublicCompetitionsLayout({ children }: { children: ReactNode }) {
  return children;
}
